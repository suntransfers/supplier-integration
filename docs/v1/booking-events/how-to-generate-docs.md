# Instructions

After updating the `index.md` file:

1. Convert it to HTML using the `Markdown PDF` extension in VS Code (`Markdown PDF: Export (html)` command).
2. Change the contents of the `<title>` tag of the generated HTML file to the same title of the MD file.